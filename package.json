{
  "name": "uniswap-v2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ epochprotocol/sushiswap-arbitrum",
    "create-local": "graph create --node http://localhost:8020/ epochprotocol/sushiswap-arbitrum",
    "remove-local": "graph remove --node http://localhost:8020/ epochprotocol/sushiswap-arbitrum",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 epochprotocol/sushiswap-arbitrum",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.64.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
